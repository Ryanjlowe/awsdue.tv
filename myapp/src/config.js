// Get this to match the segment email address and userId to push content into your pinpoint

const config = {
    address: 'neil+pinpoint1@neillock.co.uk',
    userId: 'userid1'
}

export default config;